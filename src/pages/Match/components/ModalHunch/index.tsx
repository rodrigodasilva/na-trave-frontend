import * as Dialog from "@/components/Dialog";

const Modal = () => (
  <Dialog.Content>
    <Dialog.Title>Nova aposta</Dialog.Title>
    <Dialog.Close />
  </Dialog.Content>
);

export default Modal;
