import styled from "styled-components";

export const MatchPagination = styled.nav`
  display: flex;
  gap: 24px;
  justify-content: center;
  align-items: center;
  position: relative;

  svg {
    color: var(--primary);
  }
`;
