import styled from "styled-components";

export const MatchMessageInfo = styled.p`
  font-style: normal;
  font-weight: 400;
  font-size: 14px;
  line-height: 16px;
  color: var(--gray-500);
  text-align: center;
  padding: 32px;
`;
