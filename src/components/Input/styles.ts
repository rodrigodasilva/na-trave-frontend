import styled from "styled-components";

export const Input = styled.input`
  background: #ffffff;
  border: 1px solid var(--neutral-wave);
  border-radius: 8px;
  color: var(--neutral-city);
  font-size: 14px;
  height: 40px;
  padding: 10px 12px;
`;
